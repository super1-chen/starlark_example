name: CI

on:
 push:
   branches: [ main ]
 pull_request:
   branches:
   - main
jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: Setup Bazel
        uses: abhinavsingh/setup-bazel@v3
      - name: Bazel Build
        if: matrix.os != 'windows'
        run: bazel build //...
      - name: Bazel Test
        run: bazel test //...
