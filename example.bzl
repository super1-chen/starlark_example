def greet(name):
    print("hello " + name + "!")

greet("world")

def twice(x):
    x = x * 2
    print(x)
    return x
def idiv(x, y):
  return x // y

print(idiv(6, 3))

str(twice)
twice(2)
twice("two")
